# Vendored code folder

This folder contains third-party source copied into the project so live play needs no package download.

- `README.md` is this folder map.
- `__init__.py` marks this directory as a private Python package.
- `treys/` is the vendored poker hand evaluator.
